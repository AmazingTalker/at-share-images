# at-share-images

社群貼文用的圖片（目前只有 Threads 老師推薦文的個人頁截圖）。

- 由 `lib/threads-api/scripts/upload-github.sh` 自動 push
- 這個 branch **只放圖**，沒有原始碼
- 圖片透過 `raw.githubusercontent.com` 給 Meta 抓 —— 所以這個 repo 必須是 public
- ⛔ 不要手動放任何內部資料進來
